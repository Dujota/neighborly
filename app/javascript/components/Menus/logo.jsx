import React, { memo } from 'react';

const Logo = () => (
  <div className="logo-container">
    <i className="fas fa-hands-helping logo fa-3x" alt="Neighborly Logo"></i>
  </div>
);

export default memo(Logo);
