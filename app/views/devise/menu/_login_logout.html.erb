<% if user_signed_in? %>
  <li>
  <%= link_to("Logout", destroy_user_session_path, { method: :delete, class: "login-out" }) %>
  </li>
<% else %>
  <li>
  <%= link_to("Login", new_user_session_path, class: "login-out") %>
  </li>
<% end %>