<% flash.each do |key, value| %>
	<div class="flash <%= key %> shadow">
		<%= value %>
		<div class="close-flash-container">
			<%= link_to "#", :class => "close-flash-btn", title: "Close" do %>
        Close
      <% end %>
		</div>
	</div>

<% end %>